# Phase 11 - PostgreSQL Fetch and Setup

## Added
- script to fetch PostgreSQL Windows x64 binaries from the EDB binaries page
- script to unpack and stage PostgreSQL into the portable `postgres/` directory
- script to initialise and start the local PostgreSQL runtime

## Notes
- default target version is PostgreSQL 17.9
- this phase does not yet install PostGIS
