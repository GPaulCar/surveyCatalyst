# Phase 20 - Layer Panel Flow

## Added
- layer management service
- layers panel service
- layer selection bridge
- console renderer opacity support

## Result
- layer panel can load grouped layers
- layer visibility toggles drive map controller
- opacity changes are tracked through shared selection flow
